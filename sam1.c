#include <stdio.h>
int main(){
    char name[20];        // character type array with the name name
    printf("Enter name: ");
    scanf("%s",name);
    printf("Your name is %s.",name);
    return 0;
}
